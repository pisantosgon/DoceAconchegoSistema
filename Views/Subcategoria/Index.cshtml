@model IEnumerable<Doceaconchego.Models.Subcategoria>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeSubcategoria)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Categoria)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeSubcategoria)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Categoria.CategoriaId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SubcategoriaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SubcategoriaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SubcategoriaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
